export const runtime = 'edge';        // ← Edge runtime opt‑in

import WithdrawalOptionClient from '@/components/WithdrawalOptionClient';

export default function WithdrawalOptionPage() {
  return <WithdrawalOptionClient />;
}